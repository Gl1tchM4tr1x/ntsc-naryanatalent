{
  "name": "ntsc-portal-server",
  "version": "1.0.0",
  "private": true,
  "description": "Backend for the Narayana Student Portal snapshot: persistent weekly timetable generation and daily attendance updates.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "node-cron": "^3.0.3"
  }
}
